"""Core domain layer.

This package contains the innermost layer of the application: domain models,
abstract interfaces (ports), domain exceptions, and the run state machine.
It has no dependencies on outer layers (services, infrastructure, cli).
"""
